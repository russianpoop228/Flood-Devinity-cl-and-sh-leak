ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end

ITEM.NoPreview = true
ITEM.Covers = COVERS_PLYMDL
ITEM.PersistOnDeath = true

function ITEM:OnEquip(ply, modifications)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:OnHolster(ply)
	hook.Run("PlayerSetModel", ply)
end

function ITEM:PlayerSetModel(owner,mods,ply)
	if ply != owner then return end
	owner:SetPlayerModel(self.Model, self.Skin, self.BodyGroup)

	return true
end